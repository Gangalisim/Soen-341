language: node_js
node_js: stable
cache: yarn

install:
  - yarn ci

script:
  - yarn test
  - yarn run build

deploy:
  provider: pages
  cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master